# Round 7 Restore And Merge Release Note Draft

- checkpoint restore now executes against live engine/db state
- branch merge previews and restore doctor checks
- inspector actions can dispatch restore and merge-adjacent commands directly
