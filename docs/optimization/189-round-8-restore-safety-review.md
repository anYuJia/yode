# Round 8 Restore Safety Review

## Scope

这份文档对应 round-8 tracker 的 `046`，总结 `041-045` 完成后 restore safety 的状态。

## What Closed

- restore conflict severity model
- restore and merge rollback anchors
- rollback preview command
- restore doctor now checks rollback availability

## Conclusion

- restore/merge safety is no longer only descriptive; it now leaves rollback evidence.
