# Round 9 Release Note Draft

- `Yode` 现在把 remote queue dispatch/result 和 transport control 升级成了 first-class tools，不再只依赖 `/remote-control` 命令桥接。
- permission governance、hook defer、sub-agent/team runtime、managed settings / MCP、remote live session 都已形成 inspectable runtime plane。
- tool/operator surface 统一补上了 taxonomy、monitor/follow UX、permission mode operator guide 和完整 verification docs。
