# Round 5 Runtime Orchestration Next Step

1. Promote workflow/coordinator execution from artifact logging to a real runtime state machine.
2. Merge orchestration artifacts with task/runtime state so operators can jump from plan to live execution.
3. Add direct actions from inspectors instead of relying on copyable command strings as the final interaction layer.
