# Round 4 Operator Quickstart

1. Start with `/status` and `/brief`.
2. Use `/tasks summary`, `/tasks latest`, `/memory latest`, and `/reviews latest` to jump into the active workspace.
3. Export `/doctor bundle` or `/export diagnostics` when handing off a bug or runtime incident.
