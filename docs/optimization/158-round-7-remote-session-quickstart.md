# Round 7 Remote Session Quickstart

1. Start from `/remote-control latest`.
2. Open `/remote-control queue`.
3. Run queue items with `Ctrl+Enter` or `/remote-control run latest`.
4. Use `/remote-control retry latest`, `/remote-control ack latest`, and `/remote-control handoff latest` as needed.
