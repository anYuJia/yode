# Round 8 Action UX Quickstart

1. Use `Tab`/`Shift+Tab` until the inspector focuses actions.
2. Use `Left`/`Right` to cycle actions.
3. Use `Enter` to load, `Ctrl+Enter` to dispatch.
4. Reopen `/inspect artifact latest-action-history` to review what ran.
