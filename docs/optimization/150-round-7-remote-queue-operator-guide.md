# Round 7 Remote Queue Operator Guide

1. Open `/remote-control queue`.
2. Use `Ctrl+Enter` on queue actions to run, retry, or acknowledge the latest queue item.
3. Inspect the generated `remote-queue-execution` artifact after each run.
4. If a queue item fails repeatedly, switch to `/remote-control tasks` and `/remote-control handoff latest`.
