# Parity Fixture Usage Note

Use fixture generators only for scaffolded parity cases. Promote a fixture into long-lived use only after:

- generator output is validated
- minimization has run
- inventory is refreshed
- owner routing is clear in the manifest
