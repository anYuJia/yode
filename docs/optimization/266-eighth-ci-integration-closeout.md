# Eighth CI Integration Closeout

The repository CI now includes:

- parity snapshot job
- parity replay job
- parity visual/docs job
- stable artifact names and upload paths
- explicit cache, timeout, permissions, and concurrency policy
