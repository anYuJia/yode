# Round 7 Action Dispatch Quickstart

1. Open an inspector that has an action row.
2. Use `Enter` to load the highlighted action command into input.
3. Use `Ctrl+Enter` to dispatch the action immediately.
4. Prefer read-only or preview actions before write-capable rerun or restore actions.
