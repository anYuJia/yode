# Round 6 Remote Task Operator Guide

1. Start from `/remote-control tasks`.
2. Use `/remote-control follow latest` to preload a task follow prompt.
3. Use `/remote-control retry-summary` before retrying a failed remote task.
4. Use `/remote-control handoff latest` to package a task for another operator or later continuation.
