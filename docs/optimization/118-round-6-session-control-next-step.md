# Round 6 Session Control Next Step

1. Add real checkpoint restore into engine and persisted db state.
2. Add branch merge / fork semantics instead of snapshot-only branch artifacts.
3. Promote rewind from safety preview to reversible session control primitive with explicit confirm flow.
