# Round 8 Remote Queue Runtime-Task Quickstart

1. Open `/remote-control queue`.
2. Run the selected item.
3. Jump from queue item to runtime task via `/tasks latest`.
4. Use `/remote-control handoff latest` if the queue item still needs manual follow-through.
