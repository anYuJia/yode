# Round 6 Action Quickstart

1. Open any orchestration or checkpoint artifact in the inspector.
2. Use the action row first, footer second.
3. Prefer `diff`, `history`, `doctor`, and `bundle` actions before any rerun or write-capable action.
4. Treat actions as guided command bridges for now; they are visible affordances, not yet one-key execution.
