# Round 8 Restore Safety Quickstart

1. Run `/doctor restore`.
2. Use `/checkpoint restore-dry-run latest` or `/checkpoint branch merge-dry-run latest`.
3. Execute restore or merge.
4. Inspect rollback anchors before any follow-up rewrite.
